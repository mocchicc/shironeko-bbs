
<div ng-controller="PostsCtrl">

  <form ng-submit="create()">
  <div class="row">
    <div class="col-xs-4">
      <input type="number" pattern="\d*" class="form-control" id="exampleInputEmail2" placeholder="12345" ng-model="newPost.body" required>
    </div>
    <div class="col-xs-5">
      <input type="text" class="form-control" id="exampleInputName2" placeholder="ダグラス4凸" ng-model="newPost.author" required>
    </div>
    <div class="col-xs-2">
    <button type="submit" class="btn btn-default">投稿</button>
    </div>
  </div>
  </form>

  <div class="post" ng-repeat="post in posts | limitTo:20 | orderBy:'-created_at'">
    <!--<span>{{$index}}.</span>-->
    <div class="body">{{post.body}}</div>
    <span class="author">by {{post.author}}</span>
    <span class="time">{{post.created_at | date: 'MM-dd HH:mm:ss'}}</span>
  </div>

  <span>※ 投稿は最新20件まで表示されます</span>

</div>

